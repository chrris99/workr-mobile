import { ButtonSize } from "./buttons";
import { TextType } from "../typography/fonts";

export const buttonText: Record<ButtonSize, TextType> = {
  sm: 'body-S-semibold',
  md: 'body-S-semibold',
  lg: 'body-M-semibold',
  xl: 'body-M-semibold',
  "2xl": 'body-L-semibold'
};
